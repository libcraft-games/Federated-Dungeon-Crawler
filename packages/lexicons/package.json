{
  "name": "@realms/lexicons",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "generate": "lex build --lexicons ../../lexicons --out src/lexicons --indexFile --importExt .js --clear",
    "build": "bun run generate && tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@atproto/lex": "^0.0.20"
  },
  "devDependencies": {
    "@atproto/lex-cli": "^0.9.9"
  }
}
