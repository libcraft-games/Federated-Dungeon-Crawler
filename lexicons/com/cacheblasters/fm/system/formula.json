{
  "lexicon": 1,
  "id": "com.cacheblasters.fm.system.formula",
  "defs": {
    "main": {
      "type": "record",
      "key": "any",
      "description": "Defines a derived stat formula. Record key is the derived stat ID (e.g. 'maxHp', 'maxMp', 'maxAp', 'carryWeight').",
      "record": {
        "type": "object",
        "required": ["name", "expression"],
        "properties": {
          "name": { "type": "string", "maxLength": 64 },
          "description": { "type": "string", "maxLength": 512 },
          "expression": {
            "type": "string",
            "maxLength": 1024,
            "description": "Formula expression. Variables: 'level', any attribute ID (e.g. 'str', 'con'), and constants. Example: '20 + (level - 1) * 8 + con / 2'"
          },
          "min": { "type": "integer", "description": "Minimum result value" },
          "max": { "type": "integer", "description": "Maximum result value" }
        }
      }
    }
  }
}
